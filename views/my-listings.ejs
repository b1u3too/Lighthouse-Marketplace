<!DOCTYPE html>
<html lang="en">

<head>
  <title>My Listings</title>
  <%- include('partials/head'); %>
</head>

<body>
  <header>
    <%- include('partials/nav')%>
  </header>

  <div class="listings-banner">
    <h2>Your Listings</h2>
    <!-- TODO: Define the endpoint taking a person to the new-listing page -->
    <a href=#>New Listing</a>
  </div>

  <section class="my-listing-feed">
    <% for (let listing of listings) { %>
      <article>
        <!-- TODO: another location that needs broken image link help -->
        <img href="#">
        <div class="listing-preview">
          <p> <%= listing.title %></p>
          <p> <%= listing.description %></p>
          <p> $<%= listing.price %>.00</p>
        </div>
        <div class="listing-options">
          <!-- TODO: these anchor endpoints will need some definition! -->
          <a href=#>Edit</a>
          <!-- IDEA: Could we use JQuery to give the user some kind of confirmation (sold: congrats! de-list: are you sure? Okay bye!) and refresh the page a second later? That might make for a nicer user experience! -->
          <a href=#>Mark Sold</a>
          <a href=#>De-list</a>
        </div>
      </article>
    <% } %>
  </section>
</body>

</html>
