<!DOCTYPE html>
<html lang="en">

<head>
  <title>Home Page</title>

<%- include('partials/head'); %>
</head>

<body>
  <% for (let item of items) { %>
    <a href="/api/items/<%=item.id%>"><%= item.id %></a>
    <p> <%= item.name %></p>
  <% } %>
</body>

</html>
